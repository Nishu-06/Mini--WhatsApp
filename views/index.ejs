<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="style.css"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
     rel="stylesheet" 
     integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
     crossorigin="anonymous">
</head>
<body style="background-color:rgb(237, 241, 243);">
    <h3>All Chats!</h3>
    <form method="GET" action="/chats/new"  class="py-2 px-2">
        <button class="btn btn-primary rounded-pill btn-lg">New Chat</button>
    </form>
    <%for(let chat of chats){%>
    <div id="chat">
        <p>From:<i><%=chat.from%></i></p>
        <div class="msg">
            <p><%=chat.msg%></p>
            <form method="GET" action="/chats/<%=chat._id%>/edit">
                <button class="btn bg-white rounded-pill btn-custom-sm ">Edit</button>
            </form>
            <form method="POST" action="/chats/<%=chat._id%>?_method=DELETE">
                <button class="btn bg-white rounded-pill btn-custom-sm">Delete</button>
            </form>        
        </div>
        <p>Received by:  <i><%=chat.to%></i></p>
        <hr>
        <p><%=chat.created_at.toString().split(" ")[4]%></p>
        <p><%=chat.created_at.toString().split(" ").slice(0,4).join("-") %></p>
    </div>
    <br/>
    <br/>
<%}%>
</body>
</html>